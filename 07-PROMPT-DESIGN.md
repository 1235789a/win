# 07 · prompt 设计哲学 + 迭代史

> 这个系统的命门是 prompt 不是代码。接手 agent 在改 `src/lib/prompts.ts` 之前**务必读这份文档**。

---

## TL;DR

- **目标**：让 AI 输出"独立开发者明天就能 commit"的 brief，不是"咨询公司 deck"。
- **现状**：v4 prompt，实测效果 OK（见 `samples/latest-brief-upwork.md`）。
- **核心手段**：强角色 + 禁词黑名单 + 章节结构锁死 + 具体例子植入。
- **改之前三问**：新改的内容在旧测试用例上退化了吗？禁词列表动了吗？自查章节（第 8 节）还在吗？

---

## 设计三原则

### 原则 1：角色要强到能对抗"安全模板"倾向

LLM 默认会退化到"人畜无害的通用模板"。单纯说"写一份技术蓝图"，10 次有 9 次得到：
- Next.js + Tailwind + Stripe
- 标准 SaaS 架构
- 用户系统 / 权限 / 订阅
- Day 1-7 做 SEO + 社媒 + 博客

这些内容对**任何**痛点都成立，因此对**你这个**痛点没价值。

v4 的角色设定：

> 你是一名**正在独立造产品的顶级全栈工程师**，不是咨询公司写 deck 的人。
> 目标：另一个独立开发者读完你的 brief，**2 小时内打开 IDE 开始 commit**。

关键词："独立造产品的"、"另一个开发者"、"2 小时内 commit"——把模型从咨询视角拉回造物视角。

### 原则 2：章节结构锁死 + 每节必须跟"这一个具体痛点"绑定

v4 的 8 节：

| # | 章节 | 防退化机制 |
|---|---|---|
| 1 | 核心魔法（Core Magic） | 必须含 30-80 行可跑代码，拒绝伪代码 |
| 2 | MVP 最小切片 | 必须写"用户第一次看到效果的 30 秒" |
| 3 | 差异化 / 壁垒 | 必须列 2-3 个真实产品名，否则砍分 |
| 4 | 数据层 | 只写特有的 1-3 张表，禁止 user 八股 |
| 5 | 定价与转化 | 必须给具体数字 + 付费触发瞬间 |
| 6 | 前 10 个付费用户从哪来 | 禁止 SEO / 社媒废话，必须给可发的帖子样例 |
| 7 | 防滥用 | 2-3 个关键点 + 真实代码 |
| 8 | 可执行度自查 | AI 自己回答"跟痛点绑死了吗？列 7 条 commit message" |

每节都带"防退化锁"。任一锁破了就整节退化为废话。

### 原则 3：禁词黑名单

把 LLM 写废话时最爱用的词堆一起，让它在"想用这些词"时自己触发警觉：

```
"标准 SaaS 架构"、"现代 Web 开发"、"最佳实践"、
"撰写博客文章"、"社媒推广"、"内容营销"、"KOL 合作"、
"Next.js + Tailwind + Stripe"（单独出现不带具体上下文）、
"TODO: your logic here"、"// 你的业务逻辑"
```

这个列表要**随观察到的退化模式继续加**。每次发现一个新废话，加进来。

---

## 迭代历史

### v1（废）
"你是一个商业分析师，请给出 SaaS 建议。"
→ 结果：全部是"做一个 AI 工具，使用 OpenAI API，用户付费订阅"。

### v2（半废）
加了评分维度（痛点 / 高频 / 自动化 / SEO / 社媒 / 变现），要求输出 JSON。
→ 结果：JSON 能对，但 suggested_tool 永远是"AI Tool"，content 永远是"分析用户需求，生成内容"。

### v3（接近可用）
加了"Build Once / Sell Infinite / Zero Support"判据 + niche 具体化要求。
→ 结果：打分有合理分层了，但 blueprint 还是"八段锦"：
- 技术栈：Next.js + CF + D1
- 架构：Frontend / Backend / DB
- 7 天时间表：Day 1 立项、Day 7 部署
**换任何痛点章节都一样**。

### v4（当前版本，已验证）
三大改动：

1. **拒绝架构师视角，切回造物者视角**：从"咨询公司写 deck 的人" → "正在独立造产品的顶级全栈工程师"
2. **章节结构全部重写**：前三节改成"核心魔法 / MVP 切片 / 差异化"，把"这个产品凭什么 work"放到最显眼位置
3. **加禁词黑名单 + 自查节**：第 8 节让 AI 自查是否退化，自查不过关相当于模型自己 flag 自己

实测产出：`samples/latest-brief-upwork.md`——Upwork 痛点，得分 90，9800 字，含真实 `analyzeProposal()` 算法代码、具体到 subreddit 的冷启动计划、2 张真实数据表 DDL、7 条 commit message。

---

## 改 prompt 的安全操作清单

改之前：

- [ ] 跑 `node scripts/test-one.mjs` 记录当前 Upwork 痛点的 score + blueprint 长度
- [ ] `git diff` 看清楚自己要改哪里
- [ ] 改的是否动了以下"保险丝"？
  - 角色定义（"独立造产品的顶级全栈工程师"）
  - 8 节章节结构
  - 禁词黑名单
  - 第 8 节自查
  - "score < 80 → blueprint = """ 的硬规则

改之后：

- [ ] 再跑一次 Upwork 痛点，score 不应 < 80，blueprint 不应 < 5000 字
- [ ] 再跑 2 个回归测试：一个明显灰产（应得 < 30），一个明显价值痛点（应得 > 80）
- [ ] 如果退化了 → 立刻回滚，找出是哪个改动导致的

建议 prompt 也进 git，这样能随时 bisect。

---

## 不应该做的改动

- ❌ 把禁词黑名单改短："已经很稳定了不需要"——立刻退化
- ❌ 把 8 节合并成 5 节："章节太多"——每缩减一节就塌一半
- ❌ 把自查节去掉："自查不好看"——是防自嗨的最后一道
- ❌ 温度调到 0："要稳定"——temperature=0 下 Gemini 更爱复读模板，0.2-0.5 最好
- ❌ 放宽到英文 prompt："换英文更准"——当前 niche 高度中文本地化，英文反而退化

---

## 可以做的改动方向

- ✅ 给更多"好样例 / 坏样例"对比块（few-shot 学习）
- ✅ 给"这个痛点背后的常见 4 种 niche 假设"结构化提示
- ✅ 加"你输出前必须先检查的 3 个问题"前置思考块
- ✅ 改 `deep-prompts.ts`（二次加工管线用）让它从现有 opportunity 衍生出 3 条变体产品创意

---

## Few-shot 槽位（未来可以加）

现在的 v4 完全没有 few-shot，靠"强角色 + 章节结构 + 禁词"撑着。如果哪天退化，可以加一个 USER 消息的前半段：

```
【好的 blueprint 样例】
痛点：Upwork 提案筛选
好的核心魔法：analyzeProposal(job, proposal) → 基于 embedding 对比 ...
好的 MVP：3 个功能，精确到"粘贴 URL → 30 秒看到 Top 3"
...

【坏的 blueprint 样例】
痛点：任意痛点
坏的核心魔法：使用 OpenAI API 分析文本 ← 空洞
坏的 MVP：完整 SaaS 架构 ← 不是 MVP
...

现在请按【好的】标准输出：
```

加 few-shot 成本是多占 1500-2000 token 的 input，但对退化免疫力会更强。要不要加取决于你实测 v4 能不能稳住。
