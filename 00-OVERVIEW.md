# 00 · 产品定位

## 一句话定义

> 把散落在 Reddit / X / HN / Product Hunt 上的"真实抱怨"
> 转成"独立开发者今天就能 commit 的产品 brief"的管线。

## 不是什么（Anti-Definition）

- ❌ 不是新闻聚合器
- ❌ 不是 AI 摘要工具
- ❌ 不是"让我看一下今天 AI 圈发生了啥"的 TL;DR 服务
- ❌ 不是泛 SaaS 创业课套路

## 是什么

一个**判断器 + 生成器**的组合：

```
一段真实用户抱怨
        ↓
    判断器（痛点够痛吗？够高频吗？能 build once 无限复卖吗？）
        ↓
   得分 < 80 → 记录但不展开（省 token）
   得分 ≥ 80 → 生成器输出 9000+ 字落地蓝图
        ↓
   入库 → Dashboard 展示 → 人去挑哪个值得做
```

---

## 目标用户（Owner）

**这个系统是给"造产品的人"用的**，不是给投资人 / 分析师 / 内容运营用的。

理想用户画像：
- 一个人 / 小团队的独立开发者
- 面向海外市场（英文产品线）
- 每周能抽 10-20 小时做 side project
- 已经有 1-2 个小产品上线（不是完全新手）
- 缺的不是"怎么做"，是"做啥才值"

## 终端客户（系统产出的 brief 服务谁）

系统生成 brief 时，瞄准的"理想买单人"画像：
- **海外华人出海业务主**：东南亚跨境卖家 / 北美 DTC 站 / 北美本地服务商 / 代写留学工作室 / 华人独立开发者同行
- 痛点足够具体（不是"想省钱"，是"Upwork 看 50 个 proposal 2 小时受不了"）
- 有预算但没团队（这是 $49 一次性付费的甜点）

---

## 判断框架：Build Once / Sell Infinite / Zero Support

一个真正值得做的机会，必须**同时满足** 4-5 条：

1. 一次开发，无限复制售卖（不用人介入每单）
2. 客服工作量 ≈ 0（FAQ + 自助面板覆盖 95%）
3. 单次交付完整价值（不是咨询 / 不是长期外包）
4. 30 秒内让用户感知"卧槽这真有用"
5. 痛点足够窄 + 足够痛 + 足够高频

| 命中条数 | 得分区间 | 对应动作 |
|---|---|---|
| 4-5 条 | 75-100 | 生成完整 blueprint |
| 2-3 条 | 40-74 | 存库但不展开 |
| 0-1 条 | 0-39 | 灰色记录，大概率垃圾 |

---

## 灰产禁令（硬红线）

内容若暗示：
- 绕过平台风控 / 伪造指纹
- 爬付费数据 / 破解他人系统
- 代刷代冲 / 虚假流量

→ 自动判定 score ≤ 20，并在分析首句标注「灰产陷阱」。

prompt 层已写死这条规则，别轻易放宽。

---

## 当前阶段：MVP 验证完毕

已跑通的闭环：
```
贴痛点 → AI 分析（单次 35s）→ 落库 → Dashboard 展示 → blueprint 可读可执行
```

跑过 7 条真实痛点，最高分 92，最低 30+。Upwork 那条（id=7, score=90）是代表作，见 `samples/latest-brief-upwork.md`。

**还没做**的东西全部列在 `06-ROADMAP.md`。
